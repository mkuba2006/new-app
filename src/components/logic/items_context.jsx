import React from 'react';
const Mod_elements = React.createContext({
  // items: [],
  // totalAmount: Number(0),
  folders: [],
  addFolder: (name)=>{},
  removeFolder: (name)=>{},

  addItem: (item) => {},
  // removeItem: (id) => {}

});

export default Mod_elements;